name: Jest Runner (CI)

on:
  push:
    branches:
      - 'martian-robots'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install modules
        run: yarn install && yarn add -D vitest
      - name: Run tests
        run: yarn test:unit
      - name: Run coverage report
        run: yarn coverage-ci
      - name: Make README badges
        run: yarn make-badges